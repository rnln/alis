add_newline = false

[line_break]
disabled = true

[git_status]
ahead = "↑"
behind = "↓"
deleted = "✗"
diverged = "∥"
renamed = "→"

[python]
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
